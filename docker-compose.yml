services:

  backend:
    build:
      context: ./backend
    container_name: weed-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: weed-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
